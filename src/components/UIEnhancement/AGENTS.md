# UI 增强组件 (UIEnhancement)

## 说明
提供动画卡片、响应式网格等 UI 增强组件。

## 核心文件
- AnimatedCard.tsx: 动画卡片
- ResponsiveGrid.tsx: 响应式网格

## 开发要点
- 动画使用 CSS transition/animation，优先 GPU 加速属性
- 网格支持断点配置，对齐 antd Grid 断点
- 卡片交互状态（悬停/点击/展开）视觉统一
